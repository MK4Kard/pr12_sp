<Page x:Class="pr12_vUser.Pages.RoleForm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pr12_vUser.Pages"
      xmlns:validators="clr-namespace:pr12_vUser.ValidationRules"
      xmlns:converters="clr-namespace:pr12_vUser.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RoleForm">
    <Page.Resources>
        <converters:InverseAndBooleansToBooleanConverter x:Key="boolConverter"/>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <StackPanel>
                <Label>Название роли</Label>
                <TextBox x:Name="title">
                    <TextBox.Text>
                        <Binding Path="Title" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:EmptyValidation
                                    ValidatesOnTargetUpdated="True"/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </StackPanel>
            <StackPanel Margin="0 10 0 0">
                <Button Click="save" Content="Сохранить">
                    <Button.IsEnabled>
                        <MultiBinding Converter="{StaticResource boolConverter}">
                            <Binding ElementName="title" Path="(Validation.HasError)"/>
                        </MultiBinding>
                    </Button.IsEnabled>
                </Button>
                <Button Click="back">Назад</Button>
            </StackPanel>
        </StackPanel>
        <ListView Grid.Column="1"
                  ItemsSource="{Binding Users}" Foreground="#FF9DFF50">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Foreground="Black">
                        <Run Text="Login: "/>
                        <Run Text="{Binding Login}"/>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
